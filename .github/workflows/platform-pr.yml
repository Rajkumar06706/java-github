name: Platform PR

on:
  workflow_call:
    inputs:
      java-version:
        required: true
        type: string
jobs:

  quality:
    uses: Rajkumar06706/java-github/.github/workflows/java-quality-pr.yml@main
    with:
      java-version: ${{ inputs.java-version }}

  security:
    uses: Rajkumar06706/java-github/.github/workflows/codeql-pr.yml@main
    with:
      java-version: ${{ inputs.java-version }}

  #ai-review:
    #needs:
     # - quality
     # - security
    #uses: Rajkumar06706/java-github/.github/workflows/ai-review-pr.yml@main

